exp: MinimalPair

mode: 
    - analyze

models:
    hf_causal_model:
      - gpt2

predfpath: ./predictions/minimal_pairs_bad.tsv

datafpath: ./data/minimal_pairs_bad.tsv

resultsfpath: ./results/minimal_pairs_bad.tsv

save: by_word, by_pair, by_cond

conditions: condition, blah, 